$(function () {
    "use strict";
    
    /*
    $(".latest-blog .box").on({
        mouseenter: function () {  
            let subtitleHeight = $(this).find(".subtitle").innerHeight() + 10;
            $(this).find(".text").css({
                transform: "translateY("+ subtitleHeight +"px)"
            });
        },
        mouseleave: function () {  
            
            $(this).find(".text").css({
                transform: "translateY(0px)"
            });
        }
    });
    */
});
